
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: anki-backend-prod
    ports:
      - "8000:8000"
    environment:
      - PORT=${PORT:-8000}
      - CORS_ALLOW_ORIGINS=${CORS_ALLOW_ORIGINS:-*}
    env_file:
      - .env
    networks:
      - anki-network
    restart: always
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-}
    container_name: anki-frontend-prod
    ports:
      - "${FRONTEND_PORT:-8080}:${FRONTEND_PORT:-8080}"
    environment:
      - PORT=${FRONTEND_PORT:-8080}
    env_file:
      - .env
    depends_on:
      - backend
    networks:
      - anki-network
    restart: always

networks:
  anki-network:
    driver: bridge
